% However it seems to be much more premissive than the other two algorithms and generally detects more coldwater patches than the other two algorithsm. There however still is a reasonable agreement. But it has the benefit, that it does not produce any sharp cutoffs, as the new reference temperature raster is continues. This just shows very much, that reoperationalisation of one concept allready procduces rather different cwp results. There might be a polethora of differnt implementation possibilities for a cwp algorithm, and here only two such approaches where tested out, which are rather related. But this difference in operationalsistion allready produces reasonably different outputs. And this just shows, that there is no guarentee that any of these algorithms correctly capture the underlying cwp dynamics as they all do not agree with each other. It just shows that it is somethimes very hard to model a phenomena especially here, where there is virtually no concrete and real ground truth data. (The exact boundaries of a cwp itself can not relay be mapped out in a river anyways)



